# 更新日志 Changelog

## v1.2.0 · Windows (2026-09-02)
- 「后台服务」点「▶ 启动」即开启「登录 Windows 后自动运行」（启动文件夹 .cmd 实现，
  免管理员、不弹 UAC）；「极速启动」保留为可选增强（开机未登录/锁屏即联网）
- 自启开关以程序自维护状态文件为准，勾选框与「运行情况」指示灯稳定、不再闪变
- UAC 提权子进程结果可靠回传，失败时透传 schtasks 真实报错（红字显示）
- 守护进程单实例保护加映像名校验，避免重启后后台服务起不来
- 设置页不再使用固定端口（避免与本地网站冲突）；重复打开直接置前复用已有窗口
- 成功/失败提示红绿区分、界面排版修复；卸载时清理计划任务与「启动」文件夹启动器

## v1.0.3 · Android (2026-09-02)
- 支持免认证校园网：连上即可上网的网络显示「已连接」，不再误判为需认证并反复登录
- 免认证网络不自动登录、隐藏「下线」按钮，保留「判断有误？强制认证」兜底入口
- 自动记忆免认证网络（按网关指纹）；免认证时信息卡改显本机 WiFi 的 IP / MAC

## v1.0.1 · Android (2026-08-29)
- 主界面网络状态自动刷新；非在线状态不再显示旧 IP / MAC
- 手动下线 5 分钟冷却期内不自动补登；下拉刷新时若需登录则自动登录一次

## v1.1.0 · Windows (2026-08-28)
- Python + pywebview 桌面应用，后台守护断线自动重连
- 免管理员权限安装，支持覆盖升级
