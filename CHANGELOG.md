# 更新日志 Changelog

## v1.2.0 · Windows (2026-09-02)
- 修复：本地部署其他网站时，打开本软件界面显示成那个网站的内容
  （设置页不再使用固定端口 8765，改为每次启动动态分配 + 随机令牌校验；
  关闭 Windows 上的端口抢占语义，识别自身实例时校验身份标识）
- 修复：重复双击时提示"端口被占用"，现改为把已运行的设置窗口置前复用
- 新增「极速启动」：开机即启动，锁屏状态下后台也运行（管理员授权一次，类似 Wallpaper Engine）
- 自启机制从注册表 Run 键改为系统计划任务「极速启动」（ONSTART / SYSTEM 身份，开机即触发）
- 界面简化：仅保留「极速启动」一种自启方式，运行情况面板同步精简
- 修复：设置页选项文字被压进 96px 宽度导致每行只显示两三个字的排版问题
- 开机自启提速：耗时的 WiFi 扫描/连接挪到后台线程（原最坏阻塞 55 秒）、
  开机后前 60 秒用 5 秒抢跑间隔、进程优先级提升为「高于正常」
- 新增守护进程单实例保护，避免多重启动导致重复登录互相踢下线
- 修复：单次测试模式会破坏运行中守护进程的 pid 文件，导致「停止后台服务」失效
- 修复：运行情况面板「开机自启」状态灯因 DOM id 冲突永远显示 "--"
- 安全加固：设置页所有接口要求随机令牌，阻止本机其他网页跨域调用
- 卸载程序同步清理两个自启计划任务

## v1.0.3 · Android (2026-09-02)
- 支持免认证校园网：连上即可上网的网络正确显示「已连接」，不再误判为「需认证」并反复登录报错
- 免认证网络下不再自动登录，隐藏「下线」按钮，保留「判断有误？强制认证」兜底入口
- 自动记忆免认证网络（按网关指纹），下次连上直接判定；若在该网络登录成功会自动纠正
- 免认证时信息卡改显本机 WiFi 的 IP / MAC

## v1.0.1 · Android (2026-08-29)
- 主界面网络状态自动刷新（WiFi 断开 / 重连 / 认证变化即时更新）
- 非在线状态不再显示旧 IP / MAC / 十六进制
- 下线后正确显示「需登录 / 认证」（修复网关按 IP 误判在线）
- 手动下线 5 分钟冷却期：期间不自动补登；浏览器下线立即补登
- 下拉刷新时若需登录则自动登录一次

## v1.1.0 · Windows (2026-08-28)
- Python + pywebview 桌面应用，后台守护断线自动重连
- 免管理员权限安装，支持覆盖升级
